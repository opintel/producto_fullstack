// Connection parameters
const config = {
    db: {
        host: 'localhost',
        user: 'docker',
        password: 'docker',
        database: 'gis',
        port: 5433,
    }
};

// Export
module.exports = config;